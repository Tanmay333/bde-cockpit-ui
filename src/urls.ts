const root = "/";
const selectworkers = "/selectworkers";
const home = "/home";
const orderdetails = "/orderdetails";

const urls = {
  root,
  default: home,
  selectworkers,
  orderdetails,
};

export default urls;
